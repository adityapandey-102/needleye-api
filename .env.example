NODE_ENV=development
# The app listens on API_PORT. Most hosts (Railway/Render/Fly) inject PORT
# instead -- on Railway set API_PORT=${{PORT}} (its variable-reference syntax),
# or configure the service's target port to match a fixed value here.
API_PORT=4000
# One origin, or a comma-separated allow-list (exact match, no wildcard) --
# e.g. add the machine's LAN IP for phone testing:
#   CORS_ALLOWED_ORIGIN=http://localhost:3000,http://192.168.1.8:3000
CORS_ALLOWED_ORIGIN=http://localhost:3000
# Origin of the Next.js app. Used server-side to build absolute links the user
# opens elsewhere: the password-reset email link AND the Master Tailor QR-login
# URL (the QR encodes `${WEB_APP_URL}/qr-login?token=...`). For phone/mobile
# testing on the local network, set this to the machine's LAN IP so a scanned
# QR resolves on the phone (localhost would point at the phone itself):
#   WEB_APP_URL=http://192.168.1.8:3000
WEB_APP_URL=http://localhost:3000
# fatal | error | warn | info | debug | trace
LOG_LEVEL=info
# Queries slower than this many ms are logged at WARN (slow-query observability).
SLOW_QUERY_MS=250

# Max Postgres pool connections (whole app shares one pool). Main concurrency
# lever -- raise for higher simultaneous load, keep under the DB's limit (on
# Supabase, prefer the pooler). 10 suits ~20-30 users; ~20-30 suits 60+.
DB_POOL_MAX=10
# Day of the month an accounting period begins, for the monthly revenue report
# (GET /orders/revenue). 1 = calendar months (default). Set to e.g. 7 for a
# 7th-to-6th billing cycle. Must be 1-28 (avoids month-length edge cases).
ACCOUNTING_CYCLE_START_DAY=1
# Auth-endpoint brute-force protection (in-memory): window length + max attempts
# per IP per window. Applies only to /auth/* (login, refresh, reset, qr-login, ...).
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=30

# Express `trust proxy`: how far to trust X-Forwarded-For so the rate limiter
# keys on the real client IP. A hop count ("1"), a preset ("loopback"), or
# "false". Default "loopback" trusts only same-machine proxies (the Next.js
# session proxy in dev). In production set the number of proxies in front of
# the app -- "1" on Railway/Render/Fly, which put exactly one edge proxy in
# front. Leaving it "loopback" in production makes express-rate-limit throw
# ERR_ERL_UNEXPECTED_X_FORWARDED_FOR and key on the proxy's IP, not the
# client's. Never "true" (that lets any client spoof its IP).
TRUST_PROXY=loopback

# From `npx supabase start` output in local dev; from Supabase project
# settings in prod. SUPABASE_SERVICE_ROLE_KEY is server-only -- never expose
# it to the client or commit it. SUPABASE_ANON_KEY is used by the Auth module
# for sign-in/refresh/password-reset/code-exchange (the same key a public
# client would use) -- also server-only here, since needleye-web never talks
# to Supabase directly anymore.
# NOTE: if something local (e.g. an editor's port forwarding) squats on
# 127.0.0.1:54321, auth calls hit the wrong server and return HTML. Using
# http://localhost:54321 routes via IPv6/Docker and sidesteps that collision.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Plain Postgres connection string -- every repository's actual data access
# goes through this via Drizzle (src/common/database/drizzle-client.ts), not
# through Supabase's SDK/PostgREST. The default already matches the local
# Supabase CLI stack's exposed Postgres port; swapping Postgres hosts in
# prod is just changing this one value.
#
# PROD: use the Supabase *Session-mode* pooler (port 5432 on the
# ...pooler.supabase.com host from the dashboard's "Connection string ->
# Session pooler"), NOT the direct 5432 db host and NOT Transaction mode
# (6543). Rationale: this API keeps a small long-lived pg.Pool (max 10) per
# instance and Drizzle uses named prepared statements, which Transaction mode
# does not support; Session mode gives one real backend per checked-out
# connection, so prepared statements and our pool behave exactly as they do
# against a direct connection -- no code change, just this URL. Example:
#   postgres://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres
# See docs/cutover-checklist.md.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:54322/postgres
STORAGE_BUCKET_NAME=order-images
